---
phase: 03-member-management
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/(dashboard)/members/new/page.tsx
autonomous: true
requirements: [MEMB-02]

must_haves:
  truths:
    - "Admin navigates to /dashboard/members/new and sees a form with fields: Nome, Email, CPF, Telefone (opcional)"
    - "Submitting with a blank Nome shows 'Nome é obrigatório' inline below the field"
    - "Submitting with an invalid CPF format shows 'CPF deve ter 11 dígitos' inline below the field"
    - "Submitting with duplicate email shows 'Este email já está em uso nesta academia' inline"
    - "Submitting with duplicate CPF shows 'Este CPF já está em uso nesta academia' inline"
    - "Successful submit redirects to /dashboard/members/{newId} (handled by createMemberAction)"
    - "Cancel button navigates back to /dashboard/members"
    - "Form uses Server Action — no client-side fetch"
  artifacts:
    - path: "src/app/(dashboard)/members/new/page.tsx"
      provides: "Create member form page — Client Component using useFormState with createMemberAction"
      exports: ["default MemberNewPage"]
  key_links:
    - from: "src/app/(dashboard)/members/new/page.tsx"
      to: "src/lib/actions/members.ts"
      via: "useFormState(createMemberAction, null)"
      pattern: "createMemberAction"
---

<objective>
Build the member create form page at `/dashboard/members/new`. The form collects Nome, Email, CPF, and Telefone and submits via `createMemberAction` (built in Plan 01). Inline validation errors in pt-BR appear below each field on failure.

Purpose: Delivers MEMB-02 — the primary entry point for adding members to the gym.
Output: `src/app/(dashboard)/members/new/page.tsx` — a Client Component that wires the form to the Server Action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-member-management/03-CONTEXT.md
@.planning/phases/03-member-management/03-UI-SPEC.md
@.planning/phases/03-member-management/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/lib/actions/members.ts (created in Plan 01):
```typescript
'use server'
export async function createMemberAction(
  _prevState: { error?: string } | null,
  formData: FormData
): Promise<{ error?: string }>
// On success: redirects to /dashboard/members/{newId}
// On error: returns { error: string } — pt-BR message
// Error strings returned:
//   'Nome é obrigatório'
//   'Email inválido'
//   'CPF deve ter 11 dígitos'
//   'Este email já está em uso nesta academia'
//   'Este CPF já está em uso nesta academia'
```

UI-SPEC classes (exact — copy verbatim):
- Page wrapper: `bg-gray-50 min-h-full p-8`
- Form card: `bg-white border border-gray-200 rounded-lg p-6 max-w-lg mx-auto`
- Form labels: `block text-xs font-semibold text-gray-900 mb-1`
- Inputs: `w-full h-10 px-3 border border-gray-200 rounded-lg text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-emerald-600`
- Error text: `text-red-500 text-xs mt-1`
- Primary button: `bg-emerald-600 text-white px-4 h-10 rounded-lg text-sm font-medium hover:bg-emerald-700`
- Cancel link: `text-gray-600 hover:text-gray-900 text-sm`

Copywriting (pt-BR, exact):
- Page title: "Novo Membro"
- Field labels: "Nome", "Email", "CPF", "Telefone (opcional)"
- Save button: "Salvar"
- Cancel link: "Cancelar"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Member create form page at /dashboard/members/new</name>
  <files>
    src/app/(dashboard)/members/new/page.tsx
  </files>
  <read_first>
    - src/lib/actions/members.ts — exact function signature for createMemberAction and error strings it returns
    - src/app/(dashboard)/layout.tsx — confirms this route automatically gets the sidebar; no layout.tsx needed here
    - src/app/(dashboard)/members/page.tsx — reference for page wrapper class pattern (`bg-gray-50 min-h-full p-8`)
    - .planning/phases/03-member-management/03-UI-SPEC.md — exact Tailwind classes (section: Member Create/Edit Form)
  </read_first>
  <action>
Create `src/app/(dashboard)/members/new/page.tsx`. This must be a Client Component (`'use client'`) because it uses `useFormState` from `react-dom` to receive Server Action state.

The form has four fields: Nome (required), Email (required), CPF (required), Telefone (opcional).

The error returned by createMemberAction is a single string. Display it below the form card (not below a specific field) in a red error box — the action returns a field-generic error for simplicity. This matches how Phase 2 auth forms display errors.

Full implementation:

```tsx
'use client'

import { useFormState, useFormStatus } from 'react-dom'
import Link from 'next/link'
import { createMemberAction } from '@/lib/actions/members'

function SubmitButton() {
  const { pending } = useFormStatus()
  return (
    <button
      type="submit"
      disabled={pending}
      className="bg-emerald-600 text-white px-4 h-10 rounded-lg text-sm font-medium hover:bg-emerald-700 disabled:opacity-60 disabled:cursor-not-allowed"
    >
      {pending ? 'Salvando...' : 'Salvar'}
    </button>
  )
}

export default function MemberNewPage() {
  const [state, formAction] = useFormState(createMemberAction, null)

  return (
    <div className="bg-gray-50 min-h-full p-8">
      <div className="max-w-lg mx-auto">
        <h1 className="text-2xl font-semibold text-gray-900 mb-6">Novo Membro</h1>

        <div className="bg-white border border-gray-200 rounded-lg p-6">
          <form action={formAction} noValidate>

            {/* Error banner */}
            {state?.error && (
              <div className="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg">
                <p className="text-red-600 text-sm">{state.error}</p>
              </div>
            )}

            {/* Nome */}
            <div className="mb-4">
              <label htmlFor="name" className="block text-xs font-semibold text-gray-900 mb-1">
                Nome
              </label>
              <input
                id="name"
                name="name"
                type="text"
                autoComplete="name"
                required
                className="w-full h-10 px-3 border border-gray-200 rounded-lg text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-emerald-600"
                placeholder="Nome completo"
              />
            </div>

            {/* Email */}
            <div className="mb-4">
              <label htmlFor="email" className="block text-xs font-semibold text-gray-900 mb-1">
                Email
              </label>
              <input
                id="email"
                name="email"
                type="email"
                autoComplete="email"
                required
                className="w-full h-10 px-3 border border-gray-200 rounded-lg text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-emerald-600"
                placeholder="email@exemplo.com"
              />
            </div>

            {/* CPF */}
            <div className="mb-4">
              <label htmlFor="cpf" className="block text-xs font-semibold text-gray-900 mb-1">
                CPF
              </label>
              <input
                id="cpf"
                name="cpf"
                type="text"
                inputMode="numeric"
                required
                maxLength={14}
                className="w-full h-10 px-3 border border-gray-200 rounded-lg text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-emerald-600"
                placeholder="000.000.000-00"
              />
            </div>

            {/* Telefone (opcional) */}
            <div className="mb-6">
              <label htmlFor="phone" className="block text-xs font-semibold text-gray-900 mb-1">
                Telefone (opcional)
              </label>
              <input
                id="phone"
                name="phone"
                type="tel"
                autoComplete="tel"
                className="w-full h-10 px-3 border border-gray-200 rounded-lg text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-emerald-600"
                placeholder="(00) 00000-0000"
              />
            </div>

            {/* Actions */}
            <div className="flex items-center gap-3">
              <SubmitButton />
              <Link
                href="/dashboard/members"
                className="text-gray-600 hover:text-gray-900 text-sm"
              >
                Cancelar
              </Link>
            </div>

          </form>
        </div>
      </div>
    </div>
  )
}
```

Key constraints:
- File MUST start with `'use client'` — required for useFormState and useFormStatus hooks
- `noValidate` on form element — server-side validation is authoritative; avoids duplicate browser validation bubbles
- SubmitButton as a separate component — required so useFormStatus works correctly (must be a child of the form's action context)
- CPF input uses `inputMode="numeric"` and `maxLength={14}` — accommodates formatted input `000.000.000-00`; stripCpf in the action handles normalization
- Cancel link uses `<Link>`, not `<button>` — navigates to /dashboard/members (D-12)
- No client-side validation JavaScript — server action errors are the source of truth
  </action>
  <verify>
    <automated>grep -n "use client\|createMemberAction\|Novo Membro\|Cancelar" /c/Users/GTIL/GymMVP/src/app/\(dashboard\)/members/new/page.tsx</automated>
  </verify>
  <done>
    - File exists at src/app/(dashboard)/members/new/page.tsx
    - First line is 'use client'
    - useFormState wired to createMemberAction
    - Four fields present: name, email, cpf, phone
    - Error banner renders state.error when present
    - Cancel link points to /dashboard/members
    - SubmitButton component uses useFormStatus for pending state
  </done>
  <acceptance_criteria>
    - `src/app/(dashboard)/members/new/page.tsx` exists
    - File first line contains `'use client'`
    - File contains `useFormState(createMemberAction, null)`
    - File contains `name="name"` AND `name="email"` AND `name="cpf"` AND `name="phone"`
    - File contains `Novo Membro`
    - File contains `Salvar`
    - File contains `Cancelar`
    - File contains `href="/dashboard/members"` (Cancel link target)
    - File contains `state?.error` (error display)
    - File contains `useFormStatus` (for pending state on submit button)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After plan completes:

1. Confirm file exists and is a Client Component:
```bash
grep -n "use client" src/app/\(dashboard\)/members/new/page.tsx
```
Expected: line 1 contains `'use client'`

2. Confirm Server Action is wired:
```bash
grep -n "createMemberAction" src/app/\(dashboard\)/members/new/page.tsx
```
Expected: at least one match (useFormState import/call)

3. Confirm all four form fields exist:
```bash
grep -n 'name="name"\|name="email"\|name="cpf"\|name="phone"' src/app/\(dashboard\)/members/new/page.tsx
```
Expected: 4 matches

4. Build check (no TypeScript errors):
```bash
cd /c/Users/GTIL/GymMVP && npx tsc --noEmit 2>&1 | tail -20
```
</verification>

<success_criteria>
- /dashboard/members/new renders a form with Nome, Email, CPF, Telefone fields
- Form submits via createMemberAction (Server Action) — no client-side fetch
- Error from action is displayed in a red banner above the form actions
- Cancel link returns admin to /dashboard/members
- useFormStatus disables submit button while pending (prevents double-submit)
</success_criteria>

<output>
After completion, create `.planning/phases/03-member-management/03-03-SUMMARY.md`
</output>
