---
phase: 01-project-scaffold-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - tailwind.config.ts
  - postcss.config.js
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - vitest.config.ts
  - tests/phase1/supabase-clients.test.ts
  - tests/phase1/schema.test.ts
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "Running `npm run dev` starts the app without errors on http://localhost:3000"
    - "`npm run test -- --run` exits 0 with stub tests pending (no failures)"
    - "The project uses `src/app` directory structure (App Router)"
    - "`.env.example` lists all five required env var keys"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies and test script"
      contains: "\"test\": \"vitest\""
    - path: "vitest.config.ts"
      provides: "Vitest config compatible with Next.js 14 App Router"
      contains: "vitest"
    - path: "tests/phase1/supabase-clients.test.ts"
      provides: "Test stub for Supabase client factory (INFR-02)"
    - path: "tests/phase1/schema.test.ts"
      provides: "Test stub for schema verification (INFR-03, INFR-04)"
    - path: ".env.example"
      provides: "Template listing all required env var keys"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "src/app/page.tsx"
      provides: "Root page (App Router)"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/phase1/*.test.ts"
      via: "include glob pattern"
      pattern: "tests/"
    - from: "package.json"
      to: "vitest"
      via: "scripts.test"
      pattern: "\"test\".*vitest"
---

<objective>
Initialize the Next.js 14 App Router project with TypeScript, Tailwind CSS, and the complete dependency set. Install and configure Vitest as the test framework. Create stub tests that will be fleshed out in later tasks. Produce `.env.example` documenting all required env var keys.

Purpose: Establishes the runnable codebase and test infrastructure that all subsequent plans depend on. Without this, no other tasks can execute.

Output:
- Runnable Next.js 14 App Router project (`npm run dev` works)
- Vitest config (`npm run test -- --run` exits 0)
- Stub test files for Phase 1 verification
- `.env.example` with all five required keys
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold-database-foundation/01-CONTEXT.md
@.planning/phases/01-project-scaffold-database-foundation/01-VALIDATION.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 App Router project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.js
    tailwind.config.ts
    postcss.config.js
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    .gitignore
  </files>
  <read_first>
    - C:/Users/GTIL/GymMVP/.planning/phases/01-project-scaffold-database-foundation/01-CONTEXT.md (decisions D-01 through D-15)
    - C:/Users/GTIL/GymMVP/.planning/research/ARCHITECTURE.md (project structure section, lines 141-195)
  </read_first>
  <action>
Run the following command from inside `C:/Users/GTIL/GymMVP` to scaffold the project. Since the directory already exists and may have files, use a temporary subdirectory approach: create the Next.js project directly in the current directory by running `create-next-app` with `.` as target (it will scaffold into the existing directory if it is otherwise empty of Next.js files).

```bash
cd C:/Users/GTIL/GymMVP && npx create-next-app@14 . \
  --typescript \
  --tailwind \
  --eslint \
  --src-dir \
  --app \
  --no-git \
  --import-alias "@/*"
```

If the scaffolder asks interactive questions, answer: no for `src/` (already passed `--src-dir`), use `@/*` for import alias, App Router yes.

After scaffolding, install the application dependencies:

```bash
cd C:/Users/GTIL/GymMVP && npm install \
  @supabase/supabase-js \
  @supabase/auth-helpers-nextjs \
  qrcode \
  qrcode.react \
  resend \
  @react-email/components \
  date-fns
```

Install dev dependencies:
```bash
npm install --save-dev \
  @types/qrcode \
  vitest \
  @vitejs/plugin-react \
  @vitest/coverage-v8
```

After install, open `src/app/page.tsx` and replace its entire content with:
```typescript
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <h1 className="text-2xl font-bold text-gray-900">GymRetain</h1>
    </main>
  );
}
```

In `package.json`, add the `test` script inside `"scripts"`:
```json
"test": "vitest",
"test:run": "vitest run"
```

IMPORTANT: Use `src/app/` directory structure — NOT `app/` at project root. The `--src-dir` flag ensures this (per D-01 decision area / Claude's Discretion in CONTEXT.md).
  </action>
  <verify>
    <automated>cd C:/Users/GTIL/GymMVP && node -e "const p = require('./package.json'); console.log(p.dependencies['@supabase/supabase-js'] ? 'OK' : 'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at `C:/Users/GTIL/GymMVP/package.json` with `"next": "14.*"` in dependencies
    - `package.json` contains `"@supabase/supabase-js"` in dependencies
    - `package.json` contains `"@supabase/auth-helpers-nextjs"` in dependencies
    - `package.json` contains `"vitest"` in devDependencies
    - `package.json` scripts object contains `"test": "vitest"`
    - `src/app/layout.tsx` exists (NOT `app/layout.tsx` at root — must be under `src/`)
    - `src/app/page.tsx` exists and contains `"GymRetain"` text
    - `tsconfig.json` exists
    - `tailwind.config.ts` exists
    - Verify: `grep -r "\"next\"" C:/Users/GTIL/GymMVP/package.json` returns a line containing `"14`
    - Verify: `ls C:/Users/GTIL/GymMVP/src/app/layout.tsx` exits 0
  </acceptance_criteria>
  <done>Next.js 14 App Router project is scaffolded with `src/app/` structure, all dependencies installed, `test` script present in package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Vitest and create Phase 1 test stubs</name>
  <files>
    vitest.config.ts
    tests/phase1/supabase-clients.test.ts
    tests/phase1/schema.test.ts
  </files>
  <read_first>
    - C:/Users/GTIL/GymMVP/package.json (verify vitest is installed)
    - C:/Users/GTIL/GymMVP/.planning/phases/01-project-scaffold-database-foundation/01-VALIDATION.md (Wave 0 requirements, lines 55-63)
  </read_first>
  <behavior>
    - Test 1 (schema stub): `describe('Phase 1 Schema')` with a `todo` for "organizations table exists"
    - Test 2 (schema stub): `todo` for "members table has qr_code_hash UNIQUE"
    - Test 3 (schema stub): `todo` for "RLS blocks anon SELECT on all tables"
    - Test 4 (clients stub): `describe('Supabase Clients')` with a `todo` for "createBrowserClient returns client"
    - Test 5 (clients stub): `todo` for "createServerClient accepts cookies"
    - All stubs must use `it.todo()` — they are NOT failing tests, just registered placeholders
    - `npm run test -- --run` must exit 0 (todo tests are skipped, not failures)
  </behavior>
  <action>
Create `vitest.config.ts` at `C:/Users/GTIL/GymMVP/vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'node',
    globals: true,
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json'],
    },
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

Create `tests/phase1/schema.test.ts`:

```typescript
import { describe, it } from 'vitest';

describe('Phase 1: Database Schema (INFR-03, INFR-04)', () => {
  it.todo('organizations table exists with id, name, admin_email, created_at columns');
  it.todo('members table has qr_code_hash TEXT NOT NULL UNIQUE');
  it.todo('members table has external_id TEXT nullable (per D-06)');
  it.todo('members table has last_checked_in TIMESTAMPTZ nullable');
  it.todo('checkins table has checked_in_at, ip_address, user_agent columns');
  it.todo('alerts table has triggered_at, email_sent_at, contact_marked_at, resolved_at columns');
  it.todo('RLS enabled: SELECT on members as anon returns empty result set');
  it.todo('RLS enabled: SELECT on checkins as anon returns empty result set');
  it.todo('RLS enabled: checkins allows anonymous INSERT (public check-in flow, per D-12)');
  it.todo('index exists on members(last_checked_in) (per D-08, INFR-04)');
  it.todo('index exists on checkins(member_id, checked_in_at) (per D-08, INFR-04)');
  it.todo('index exists on members(qr_code_hash) (per D-08)');
});
```

Create `tests/phase1/supabase-clients.test.ts`:

```typescript
import { describe, it } from 'vitest';

describe('Phase 1: Supabase Client Factory (INFR-02)', () => {
  it.todo('createBrowserClient returns a SupabaseClient instance');
  it.todo('createServerClient accepts cookies store and returns SupabaseClient');
  it.todo('createServiceRoleClient uses SUPABASE_SERVICE_ROLE_KEY env var (per D-09)');
  it.todo('createServiceRoleClient does NOT use NEXT_PUBLIC_SUPABASE_ANON_KEY');
  it.todo('env var NEXT_PUBLIC_SUPABASE_URL is defined and non-empty');
  it.todo('env var NEXT_PUBLIC_SUPABASE_ANON_KEY is defined and non-empty');
  it.todo('env var SUPABASE_SERVICE_ROLE_KEY is defined and non-empty (Phase 7 requirement)');
});
```
  </action>
  <verify>
    <automated>cd C:/Users/GTIL/GymMVP && npm run test:run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists at project root
    - `tests/phase1/schema.test.ts` exists
    - `tests/phase1/supabase-clients.test.ts` exists
    - `npm run test:run` exits 0 (all tests are `todo` — no failures)
    - `grep -c "it.todo" C:/Users/GTIL/GymMVP/tests/phase1/schema.test.ts` returns 11 or more
    - `grep -c "it.todo" C:/Users/GTIL/GymMVP/tests/phase1/supabase-clients.test.ts` returns 7 or more
    - `vitest.config.ts` contains `include: ['tests/**/*.test.ts'`
  </acceptance_criteria>
  <done>Vitest is configured, test stubs exist, and `npm run test:run` exits 0. The test framework is ready for concrete test implementations in Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Create .env.example and .gitignore</name>
  <files>
    .env.example
    .gitignore
  </files>
  <read_first>
    - C:/Users/GTIL/GymMVP/.planning/phases/01-project-scaffold-database-foundation/01-CONTEXT.md (D-14, D-15 env var decisions)
    - C:/Users/GTIL/GymMVP/.gitignore (if it exists — add .env.local if not already present)
  </read_first>
  <action>
Create `C:/Users/GTIL/GymMVP/.env.example` with the following exact content:

```
# Supabase — get from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key — NEVER expose client-side (per D-09)
# Required by Phase 7 cron job for RLS bypass
# Get from: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Resend Email Service — get from: resend.com > API Keys
# Free tier: 100 emails/day (sufficient for MVP)
RESEND_API_KEY=re_your-resend-api-key

# Cron Secret — generate with: openssl rand -hex 32
# Used to authenticate Vercel cron job calls to /api/cron/detect-churn
CRON_SECRET=your-32-char-hex-secret-here
```

Then open `C:/Users/GTIL/GymMVP/.gitignore` (created by create-next-app). Ensure it contains `.env.local` and `.env` on separate lines. If either is missing, append them. The file should already contain these from the Next.js scaffold — verify and add if absent.

Also verify `.env.local` is NOT tracked by git by running:
```bash
git -C C:/Users/GTIL/GymMVP ls-files --error-unmatch .env.local 2>&1 || echo "NOT TRACKED (correct)"
```
  </action>
  <verify>
    <automated>cd C:/Users/GTIL/GymMVP && grep -c "SUPABASE_SERVICE_ROLE_KEY" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` exists at project root
    - `grep "NEXT_PUBLIC_SUPABASE_URL" C:/Users/GTIL/GymMVP/.env.example` returns a match
    - `grep "NEXT_PUBLIC_SUPABASE_ANON_KEY" C:/Users/GTIL/GymMVP/.env.example` returns a match
    - `grep "SUPABASE_SERVICE_ROLE_KEY" C:/Users/GTIL/GymMVP/.env.example` returns a match
    - `grep "RESEND_API_KEY" C:/Users/GTIL/GymMVP/.env.example` returns a match
    - `grep "CRON_SECRET" C:/Users/GTIL/GymMVP/.env.example` returns a match
    - `grep "\.env\.local" C:/Users/GTIL/GymMVP/.gitignore` returns a match (env.local is git-ignored)
    - `.env.example` is NOT in `.gitignore` (it should be committed — it contains no secrets, only placeholder values)
  </acceptance_criteria>
  <done>`.env.example` documents all five required env var keys with comments. `.gitignore` protects `.env.local` from accidental commits.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `npm run dev` starts without console errors on http://localhost:3000
2. `npm run test:run` exits 0
3. `ls C:/Users/GTIL/GymMVP/src/app/` shows layout.tsx and page.tsx
4. `grep "SUPABASE_SERVICE_ROLE_KEY" C:/Users/GTIL/GymMVP/.env.example` returns a match
5. `grep "\"test\"" C:/Users/GTIL/GymMVP/package.json` shows vitest entry
</verification>

<success_criteria>
- Next.js 14 App Router project runs locally (`npm run dev`)
- Vitest test suite executes without failures (`npm run test:run` exits 0)
- All five required env var keys documented in `.env.example`
- Project uses `src/app/` directory structure
- All Phase 1 dependencies installed (`@supabase/supabase-js`, `@supabase/auth-helpers-nextjs`, `qrcode`, `resend`, `date-fns`, `vitest`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-database-foundation/01-01-SUMMARY.md` with:
- Dependencies installed and their versions
- Test framework configured (vitest)
- File structure established (`src/app/`)
- Stub tests created and their file paths
</output>
