---
phase: 01-project-scaffold-database-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04

must_haves:
  truths:
    - "http://localhost:3000 loads without console errors"
    - "Vercel deployment shows 'Ready' status after push to main"
    - "Supabase Table Editor shows four tables: organizations, members, checkins, alerts"
    - "Anon SQL query on any table returns zero rows (RLS active)"
    - "All five env vars set in both .env.local AND Vercel project settings"
  artifacts:
    - path: "supabase/migrations/001-init-schema.sql"
      provides: "Schema applied to live Supabase project"
    - path: "supabase/migrations/002-rls-policies.sql"
      provides: "RLS policies active on live Supabase project"
    - path: "supabase/migrations/003-indexes.sql"
      provides: "Indexes applied on live Supabase project"
  key_links:
    - from: ".env.local"
      to: "Supabase project credentials"
      via: "Developer fills in real values from Supabase Dashboard"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: "Vercel project"
      to: "GitHub repository main branch"
      via: "Vercel > Import Git Repository"
      pattern: "auto-deploy on push"
---

<objective>
Human verification and manual setup checkpoint. The developer applies the SQL migrations to their live Supabase project, fills in real credentials in `.env.local` and Vercel, connects the repository to Vercel, and verifies the Phase 1 success criteria.

Purpose: Plans 01-03 created all the code and SQL files. This plan bridges from code to live infrastructure. Without this checkpoint, subsequent phases will fail with connection errors or missing tables.

Output:
- Live Supabase project with four tables, RLS, and indexes
- Vercel deployment connected to main branch (auto-deploy live)
- All env vars configured in both `.env.local` (local dev) and Vercel (production)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold-database-foundation/01-CONTEXT.md
@.planning/phases/01-project-scaffold-database-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Apply SQL migrations to Supabase and fill in credentials</name>
  <files>supabase/migrations/001-init-schema.sql, supabase/migrations/002-rls-policies.sql, supabase/migrations/003-indexes.sql, .env.local</files>
  <action>
You must complete these steps manually. They require access to Supabase Dashboard and your Resend account.

**Step 1: Create a Supabase project (if you haven't already)**
1. Go to https://supabase.com/dashboard
2. Click "New project"
3. Choose a name (e.g., "gymretain"), set a strong database password, select the region closest to you
4. Wait for the project to be ready (~2 min)

**Step 2: Apply the SQL migrations**

Open Supabase Dashboard > SQL Editor > "New query"

Paste and run each file IN ORDER (one at a time, verify success before running the next):

File 1 — contents of `supabase/migrations/001-init-schema.sql`
- Paste entire content and click "Run"
- Expected: green success message, no errors

File 2 — contents of `supabase/migrations/002-rls-policies.sql`
- Paste entire content and click "Run"
- Expected: green success message, no errors

File 3 — contents of `supabase/migrations/003-indexes.sql`
- Paste entire content and click "Run"
- Expected: green success message, no errors

**Step 3: Get your credentials**

From Supabase Dashboard > Project Settings > API:
- Copy "Project URL" → this is NEXT_PUBLIC_SUPABASE_URL
- Copy "anon public" key → this is NEXT_PUBLIC_SUPABASE_ANON_KEY
- Scroll down, copy "service_role" key → this is SUPABASE_SERVICE_ROLE_KEY (keep this secret)

From Resend (https://resend.com/api-keys):
- Create or copy your API key → this is RESEND_API_KEY

Generate CRON_SECRET:
- Run: `openssl rand -hex 32`
- Or use any 32+ character random string

**Step 4: Fill in `.env.local`**
Open `C:/Users/GTIL/GymMVP/.env.local` and replace each `REPLACE_WITH_*` placeholder with the real value.

**Step 5: Verify RLS is working**
In Supabase Dashboard > SQL Editor, run this query:

```sql
SET LOCAL role = anon;
SELECT count(*) FROM members;
SELECT count(*) FROM organizations;
SELECT count(*) FROM alerts;
```

Each must return 0 (not an error — RLS is working correctly).

**Step 6: Test anonymous check-in INSERT**
```sql
SET LOCAL role = anon;
INSERT INTO checkins (org_id, member_id, checked_in_at)
VALUES (gen_random_uuid(), gen_random_uuid(), NOW());
```

Expected: INSERT SUCCEEDS (checkins allows anonymous INSERT per D-12). If it fails, re-run `002-rls-policies.sql`.
  </action>
  <verify>
    <automated>cd C:/Users/GTIL/GymMVP && grep "REPLACE_WITH" .env.local && echo "PLACEHOLDERS STILL PRESENT - fill in real values" || echo "OK - no placeholders found"</automated>
  </verify>
  <done>
Supabase Table Editor shows exactly four tables: organizations, members, checkins, alerts.
RLS anon queries return zero rows on all tables.
Anonymous INSERT into checkins succeeds.
`.env.local` has all five keys with real (non-placeholder) values.

Resume signal: type "migrations applied"
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Connect Vercel and configure environment variables</name>
  <files>vercel.json</files>
  <action>
**Step 1: Push the project to GitHub**
```bash
cd C:/Users/GTIL/GymMVP
git add .
git commit -m "feat(01): Next.js scaffold, DB migrations, Supabase clients"
git push origin main
```

If you don't have a GitHub remote yet:
1. Create a new repo at https://github.com/new (name: "gymretain", private)
2. Copy the remote URL
3. Run:
```bash
git remote add origin https://github.com/YOUR_USERNAME/gymretain.git
git branch -M main
git push -u origin main
```

**Step 2: Import project into Vercel**
1. Go to https://vercel.com/new
2. Click "Import Git Repository"
3. Authorize GitHub if prompted
4. Select your gymretain repository
5. Framework Preset: Next.js (auto-detected)
6. Root Directory: leave as `.` (project root)
7. DO NOT add env vars during import — you'll add them in Step 3
8. Click "Deploy"

Wait for first deployment to complete (~2-3 min).

**Step 3: Add environment variables to Vercel**
1. Go to your Vercel project dashboard
2. Click "Settings" > "Environment Variables"
3. Add each variable for Production + Preview environments:

| Key | Value source |
|-----|-------------|
| NEXT_PUBLIC_SUPABASE_URL | Copy from .env.local |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | Copy from .env.local |
| SUPABASE_SERVICE_ROLE_KEY | Copy from .env.local |
| RESEND_API_KEY | Copy from .env.local |
| CRON_SECRET | Copy from .env.local |

4. After adding all five, click "Redeploy" on the latest deployment (Deployments tab > three dots > Redeploy)

**Step 4: Verify deployment**
1. Open your Vercel project dashboard
2. Confirm the deployment shows "Ready" status (green checkmark)
3. Click the deployment URL (something like `https://gymretain-xxx.vercel.app`)
4. Confirm the page loads showing "GymRetain" heading
5. Open browser DevTools > Console — confirm no errors

**Step 5: Verify cron is configured**
1. In Vercel project dashboard, look for "Cron Jobs" in sidebar
2. Confirm `/api/cron/detect-churn` appears with schedule `0 6 * * *`
(Note: cron will show "Not yet run" — this is expected)
  </action>
  <verify>
    <automated>cd C:/Users/GTIL/GymMVP && cat vercel.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const ok = d.crons && d.crons.some(c=>c.path.includes('detect-churn') && c.schedule==='0 6 * * *'); console.log(ok ? 'vercel.json OK' : 'ERROR: cron config missing')"</automated>
  </verify>
  <done>
Vercel deployment URL is reachable and shows "Ready" status.
All five env vars appear in Vercel Settings > Environment Variables.
Vercel Cron Jobs page shows `/api/cron/detect-churn` with `0 6 * * *` schedule.
`git push origin main` automatically triggers a new Vercel deployment.

Resume signal: type "vercel ready"
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Final Phase 1 verification</name>
  <files>tests/phase1/supabase-clients.test.ts, tests/phase1/schema.test.ts</files>
  <action>
Run the automated test suite and then verify each Phase 1 success criterion manually.

**Automated check (run first):**
```bash
cd C:/Users/GTIL/GymMVP && npm run test:run
```
Expected: exits 0, all tests pass.

**Manual verification checklist — from ROADMAP.md Phase 1 Success Criteria:**

1. **Local dev works:**
   ```bash
   npm run dev
   ```
   Open http://localhost:3000 — "GymRetain" heading appears, no console errors.

2. **Vercel auto-deploy works:**
   Make a trivial change (e.g., add a comment to page.tsx), commit, push to main.
   Check Vercel dashboard — new deployment triggered automatically and reaches "Ready".

3. **Four tables exist in Supabase:**
   Open Supabase Table Editor. Confirm: `organizations`, `members`, `checkins`, `alerts` are all visible.
   Click `members` table — confirm `external_id` column exists and is nullable.

4. **Required indexes exist (INFR-04):**
   Run in Supabase SQL Editor:
   ```sql
   SELECT indexname, tablename
   FROM pg_indexes
   WHERE tablename IN ('members', 'checkins', 'alerts')
   ORDER BY tablename, indexname;
   ```
   Must see: `idx_members_last_checked_in`, `idx_checkins_member_id_checked_in_at`, `idx_members_qr_code_hash`

5. **RLS blocks anon queries (INFR-03):**
   Run in Supabase SQL Editor:
   ```sql
   SET LOCAL role = anon;
   SELECT count(*) FROM members;
   SELECT count(*) FROM organizations;
   SELECT count(*) FROM alerts;
   ```
   Each must return 0 (not an error).

6. **All env vars configured (INFR-02):**
   ```bash
   grep "REPLACE_WITH" C:/Users/GTIL/GymMVP/.env.local
   ```
   Must return nothing. Then confirm in Vercel Settings > Environment Variables that all five keys appear.
  </action>
  <verify>
    <automated>cd C:/Users/GTIL/GymMVP && npm run test:run 2>&1 | tail -5</automated>
  </verify>
  <done>
All six Phase 1 success criteria from ROADMAP.md are TRUE:
1. `npm run dev` starts without errors
2. Push to main triggers automatic Vercel deployment
3. All four tables exist in Supabase with correct columns including `external_id` on members
4. Three required indexes exist in Supabase
5. Anon queries on all tables return empty result (RLS active)
6. All five env vars set in both `.env.local` and Vercel project settings

Resume signal: type "phase 1 complete" if all checks passed, or describe which step failed.
  </done>
</task>

</tasks>

<verification>
Phase 1 is complete when all six success criteria from ROADMAP.md are TRUE:
1. `npm run dev` starts without errors
2. Push to main triggers automatic Vercel deployment (INFR-01)
3. All four tables exist in Supabase with correct columns (INFR-03)
4. Required indexes exist: members(last_checked_in), checkins(member_id+checked_in_at), members(qr_code_hash) (INFR-04)
5. Anon queries on all tables return empty result set (INFR-03)
6. All five env vars set in .env.local AND Vercel project settings (INFR-02)
</verification>

<success_criteria>
- `npm run test:run` exits 0 (automated)
- All four tables visible in Supabase Table Editor (manual)
- RLS anon queries return 0 rows on all tables (manual)
- Vercel deployment "Ready" with auto-deploy on push (manual)
- All five env vars in .env.local and Vercel settings (manual)
- SUPABASE_SERVICE_ROLE_KEY confirmed present in Vercel (critical for Phase 7)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-database-foundation/01-04-SUMMARY.md` with:
- Supabase project URL (public, non-secret)
- Vercel deployment URL
- Confirmation that all five env vars are set in both environments
- Confirmation that SUPABASE_SERVICE_ROLE_KEY is set (required for Phase 7)
- Any issues encountered and how they were resolved
</output>
